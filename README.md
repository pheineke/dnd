# Problems
## Adding figures with same name
When adding a figure with name or key "A" for example and you add a new figure with same name it doesnt do anything.
You can somewhat "update" the figure by this but the server doesnt react to size changes or anything else.

# Features to add
## Priority
- Editable player card/stats.
- Changable map on UI.
- Delete on right click.
- Add a "save" button to save the current state of the board.
- Add a "load" button to load the last saved state of the board.
- Add a "clear" button to clear the board.


## Secondary
- Add a "settings" button to change the size of the board.
- Add a "settings" button to change the size of the figures.
- Add a "settings" button to change the color of the figures.
